PNG Sequence Preview — 內建 ffmpeg

macOS: bin/macos/ffmpeg
Windows: bin/win/ffmpeg.exe

打包前請執行：
  ./scripts/fetch-ffmpeg-mac.sh   (macOS)
  並手動放置 bin/win/ffmpeg.exe   (Windows 發佈)

若 bin 內無 ffmpeg，面板會 fallback 至系統 PATH。
